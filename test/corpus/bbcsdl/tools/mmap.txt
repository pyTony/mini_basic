      ON ERROR IF ERR=17 CHAIN @lib$+"../examples/tools/touchide" ELSE MODE 3 : PRINT REPORT$ : END
      
      F% = OPENIN("/proc/self/maps.")
      IF F% THEN
        VDU 14
        PRINT
        REPEAT
          C$ = GET$#F%
          PRINT C$
        UNTIL C$=""
        CLOSE #F%
      ELSE
        PRINT "Sorry, this program does not run on Windows or Mac OS"
      ENDIF
      
      REPEAT WAIT 10 : UNTIL FALSE
      

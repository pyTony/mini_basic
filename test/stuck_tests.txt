# Only tests that still hang or need a real window. Re-check when a program
# is marked OK — do not leave working programs parked here.
#
# Animal, clock XOR, squares speed, dialect SDL text, BBC font, rotation
# math, unknown-syntax, agon text corpus: now phase0 (regular suite).

test_graphics_confirm
test_pygame_input_events
test_display
test_bbc_graphics.BBCGraphicsTests.test_benchmark
test_progress_runner

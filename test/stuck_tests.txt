# List of test names or prefixes that are potentially stuck/hanging
# (interactive, graphics, pygame, long-running corpus, input blocking, etc.)
# One per line, matched with startswith on the dotted name.
#
# Phase 1 rule: exclude these from broad auto runs (pytest -m phase1 / -k filters).
# Many graphics/interactive tests were removed from test_mini_basic.py during hygiene work.

test_interactive_animal
test_pygame_input_events
test_graphics_confirm
test_rotation_integration
test_bbc_graphics.BBCGraphicsTests.test_benchmark
test_bbc_graphics.BBCGraphicsTests.test_soccerball
test_animal_text_print
test_display
test_bbcsdl_corpus
test_agon_corpus
test_progress_runner  # may be meta

# Pre-existing drift (not phase0 yet):
test_unknown_syntax
# test_save_case is phase0 (green)
# test_regular_array_substitution_and_access.py — broken unittest (fixture 'self')

# Phase 1: keep graphics + interactive + long corpus in stuck list.
# Focus regression and new work on non-graphics foundation (expressions, control, DEF/PROC, file I/O, arrays, errors, dialects).
# Graphics tests to be refactored/isolated in Phase 2 (use display patches, bounded runs, internal state asserts).
#
# Additional Phase 1 hygiene notes (tests excised from god file test_mini_basic.py):
# - pygame auto-enable, tee, wait/ctrl-c loops, bbcsdl tier graphics samples, soccerball/wheel pixel verifiers
# See: test/manual/ for moved debug runners; dedicated test_*_graphics.py for future Phase 2 work.


# Windows REPL line editing + Tab completion (Unicode-safe; replaces custom msvcrt
# path for normal text mode). Unix uses the stdlib readline module instead.
#   pip install -r requirements-repl.txt
#   # or:  pip install "mini-basic[repl]"
pyreadline3>=3.5.0; platform_system == "Windows"
